fastapi==0.111.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9
Pillow==10.4.0
groq==0.9.0
reportlab==4.2.2
sqlalchemy==2.0.31
psycopg2-binary==2.9.9
aiofiles==23.2.1
httpx==0.27.0
python-dotenv==1.0.1

# SmartPoli additions — required for the manual path (Features 1-4).
# None of these need network access or an API key.
rapidfuzz==3.9.6
apscheduler==3.10.4
pytest==8.3.2
qrcode==7.4.2

# SmartPoli additions — real auth/RBAC + doctor/caregiver dashboards.
# Also no network access or API key required.
pyjwt==2.9.0
bcrypt==4.2.0

# Optional OCR path (Feature 1, image-upload) lives in a separate file —
# see requirements-ocr.txt. ocr_plugin.py loads it lazily (CLAUDE.md
# section 5: "OCR is a plugin, not a foundation"), so this base file is
# everything the app needs to boot and serve every feature except photo
# upload. Installing torch/transformers/opencv here would push a Render
# free-tier build past its build time and memory limits for no benefit —
# the manual prescription path is the one the product guarantees works.
